<Window x:Class="WinSyncScroll.VisualTestUtil.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinSyncScroll.VisualTestUtil"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Loaded="WindowLoaded"
        Closing="WindowClosing"
        Height="450"
        Width="800">
    <!-- Layout root -->
    <Grid>
        <!-- Overlay -->
        <Grid Panel.ZIndex="1000">
            <DockPanel Grid.Row="0" Grid.Column="0" LastChildFill="True">
                <Canvas>
                    <Ellipse Canvas.ZIndex="2"
                             Width="50"
                             Height="50"
                             Stroke="Black"
                             StrokeThickness="4"
                             Visibility="{Binding ScrollAreaVisibility}"
                             Canvas.Left="{Binding LastScrollWpfEllipseX}"
                             Canvas.Top="{Binding LastScrollWpfEllipseY}" />
                </Canvas>
            </DockPanel>
        </Grid>

        <!-- Main content -->
        <Grid>
            <DockPanel Grid.Row="0" Grid.Column="0" LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                    <Label Width="Auto" Content="{Binding LastScrollAbsoluteMessage}" />
                    <Label Width="Auto" Content="{Binding LastScrollWpfMessage}" />
                    <Label Width="Auto" Content="{Binding CursorPositionMessage}" />
                </StackPanel>

                <DataGrid FontSize="14" ItemsSource="{Binding ScrollEvents}"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserSortColumns="False"
                          CanUserReorderColumns="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          CanUserResizeColumns="False">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Timestamp}" IsReadOnly="True" Header="Timestamp" Width="2*" />
                        <DataGridTextColumn Binding="{Binding Name}" IsReadOnly="True" Header="Event" Width="2*" />
                        <DataGridTextColumn Binding="{Binding AbsoluteX}" IsReadOnly="True" Header="ABS X" Width="1*" />
                        <DataGridTextColumn Binding="{Binding AbsoluteY}" IsReadOnly="True" Header="ABS Y" Width="1*" />
                        <DataGridTextColumn Binding="{Binding WpfX}" IsReadOnly="True" Header="WPF X" Width="1*" />
                        <DataGridTextColumn Binding="{Binding WpfY}" IsReadOnly="True" Header="WPF Y" Width="1*" />
                        <DataGridTextColumn Binding="{Binding WheelDelta}" IsReadOnly="True" Header="Delta" Width="1*" />
                        <DataGridTextColumn Binding="{Binding VirtualKeys}" IsReadOnly="True" Header="Keys" Width="1*" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
